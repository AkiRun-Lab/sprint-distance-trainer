# Changelog — SDT（Sprint & Distance Trainer）

## v1.13.1 (2026-07-11)

ユーザー動作確認で挙がったUX不具合3件を修正。

### 🟡 修正
- **計画生成のプログレス表示をフォーム診断と統一**：`st.progress`＋別`st.empty()`の2要素方式を廃止し、フォーム診断（`_run_form_diagnosis`）と同じ「`prog = st.progress(0.0, text=...)`の1要素＋経過時間ラベル（1秒tick）」方式に統一。失敗時（watchdog打ち切り含む）は`prog.empty()`で「作成中」表示を確実に消してからエラーを出すよう修正
- **フォーム診断がフォールバックしていたら計画生成も代替モデルから開始**：診断で`gemini-3.5-flash`の混雑（503）が直前に判明している場合、計画生成でプライマリに3回リトライして時間を浪費するのを避け、代替モデル（`gemini-3-flash-preview`）から開始するよう変更。503が続けばプライマリへ切り替える（既存のフォールバック方向と対称）。実際に成功したモデルに応じて`progress_state["fallback"]`が正しくセットされるため、「代替モデルで生成しました」の注記表示ロジックは無変更で動作
- **生成・診断ボタンの二重クリック問題を修正**：STEP 2「フォームを診断する」「スキップして計画を作成する」「計画を作成する →」、STEP 3「トレーニング計画を生成する」の4ボタンを`st.empty()`プレースホルダ化。処理開始前にボタンを消し、長時間処理中に同じボタンを連打して処理が中断されるのを防止（`st.rerun()`は使わないためv1.8.4のdim overlay制約には抵触しない）

### 🔧 変更
- `planner.py`：`generate_plan()` に `start_with_fallback: bool = False` 引数を追加。モデル試行順を`stages`リストで構成し、`start_with_fallback=True`時は代替モデル→プライマリの順に反転（試行回数は先頭ステージが`MAX_RETRIES+1`、後段ステージが`PLAN_FALLBACK_MAX_ATTEMPTS`）
- `app.py`：`_run_plan_generation()` が `st.session_state.form_used_fallback` から `start_with_fallback` を算出してスレッドに渡す。`progress_state`の初期値もこれに合わせて設定

## v1.13.0 (2026-07-11)

フェーズ2（フォーム診断）に続き、トレーニング計画生成（`planner.py`）にも503自動リトライ＋モデルフォールバックとリクエストタイムアウトを追加（フェーズ3）。既存のリトライループ（`MAX_RETRIES`＋指数バックオフ・MAX_TOKENS即断念）はそのまま維持し、フォールバック経路を追加する構造にした。

### ✨ 追加
- **503自動リトライ＋モデルフォールバック**：計画生成がプライマリモデル（`gemini-3.5-flash`）で503を受け、既存のリトライ（最大`MAX_RETRIES`回・指数バックオフ）を使い切った場合のみ、フォールバックモデル（`gemini-3-flash-preview`・フォーム診断と共通）に自動切り替えし、最大`PLAN_FALLBACK_MAX_ATTEMPTS`（2）回まで試行する。429・MAX_TOKENS・EMPTY_RESPONSE・JSON失敗・タイムアウトで尽きた場合はフォールバックしない。フォールバック使用時はSTEP 3の計画表示直後・ダウンロードMarkdown冒頭に「代替モデルで計画を生成しました」の注記を表示
- **計画生成タイムアウト（10分）**：SDKデフォルトの無期限タイムアウトを明示的に600秒（`PLAN_TIMEOUT_SEC`）に設定。計画生成はフォーム診断より長時間かかるため余裕をみて長めに設定。タイムアウト検出時はリトライせず即断念（`TIMEOUT_EXCEEDED`）し、10分待った後にさらに10分待たせることを防ぐ
- **プログレス表示の強化**：計画生成中、フォールバックに入ると「混雑のため代替モデルで計画を生成中...」に表示を切り替え

### 🔧 変更
- `config.py` に `PLAN_TIMEOUT_SEC`・`PLAN_FALLBACK_MAX_ATTEMPTS` を追加（フォールバックモデルは既存の `GEMINI_ANALYZER_FALLBACK_MODEL` を流用）
- `planner.py`：`generate_plan()` の内部リトライループを `_attempt_generate_plan()` として関数化し、プライマリ・フォールバックの両方で共通利用（空レスポンスガード・MAX_TOKENS即断念・JSONパースの既存ロジックは両方に同様に適用）。`generate_plan()` に `progress_state` 引数を追加（フォールバック時に `progress_state["fallback"] = True`）
- `app.py`：`_run_plan_generation()` に `progress_state` を渡し、フォールバック中の表示切替に対応。`GENERATION_TIMEOUT_SECONDS` を240→700秒に引き上げ（単発ハング時の`PLAN_TIMEOUT_SEC`＝600秒に余裕を加味）。エラー分岐に `TIMEOUT_EXCEEDED` を追加。セッション状態に `plan_used_fallback` を追加（「最初からやり直す」リセット対象にも追加）

## v1.12.0 (2026-07-11)

RFD（ランニングフォーム診断アプリ）実装済みの堅牢性機能をフォーム診断機能に移植（フェーズ2）。背景：`gemini-3.5-flash` の503（モデル高負荷）が頻発する一方、Gemini SDKの `generate_content` はデフォルトでタイムアウトが無期限のため、混雑時に診断がハングしたまま返ってこない問題があった。

### ✨ 追加
- **503自動リトライ＋モデルフォールバック**：フォーム診断がプライマリモデル（`gemini-3.5-flash`）で503を受けた場合、10秒間隔で最大3回まで自動リトライ。それでも503が続く場合はフォールバックモデル（`gemini-3-flash-preview`）に自動切り替えし、さらに最大2回試行する。フォールバック使用時はSTEP 2の診断完了直後・STEP 3の診断結果expander・ダウンロードMarkdownに「代替モデルで診断しました」の注記を表示（診断本文には混ぜない）
- **解析タイムアウト（5分）**：SDKデフォルトの無期限タイムアウトを明示的に300秒に設定。超過時は「解析が5分を超えたため中断しました」と表示し、診断回数は消費しない
- **スクリーニングタイムアウト（60秒）**：動画チェック処理にも明示的なタイムアウトを追加
- **プログレス表示の強化**：STEP 2のフォーム診断を`_run_plan_generation`と同じ threading.Thread＋result_container方式に変更し、経過時間ベースのプログレスバーを表示（通常時／自動再試行中／代替モデル使用中でラベルを出し分け）。失敗時はプログレスバーを確実に除去してから解析中表示を残さない

### 🔧 変更
- `config.py` に `ANALYZE_TIMEOUT_SEC`・`SCREEN_TIMEOUT_SEC`・`RETRY_503_MAX_ATTEMPTS`・`RETRY_503_WAIT_SEC`・`GEMINI_ANALYZER_FALLBACK_MODEL`・`FALLBACK_503_MAX_ATTEMPTS`・`ANALYZE_EXPECTED_SEC` を追加（RFDと同名・同値）

## v1.11.0 (2026-07-11)

RFD（ランニングフォーム診断アプリ）実装済みの機能群をフォーム診断機能に移植（フェーズ1）。

### ✨ 追加
- フォーム診断結果のスコア化＋レーダーチャート：5項目（接地・骨盤体幹・腕振り・股関節伸展・上下動）を10点満点でスコア化し、STEP 2診断完了直後とSTEP 3の診断結果expanderにレーダーチャート＋総合スコアを表示
- タイムスタンプ引用：診断本文の「良い点」「改善すべき点」に、該当箇所の動画内タイムスタンプ（例：[0:15]）を付与するようプロンプトを強化
- 弱点連動CTA：診断結果から弱点カテゴリ（殿筋体幹・可動域・弾性・上半身・一般）を抽出し、STEP 3の補強グッズCTAの文言をカテゴリに応じて出し分け
- ダウンロードMarkdownに「診断スコア」テーブル（項目別スコア＋総合スコア）を追加

### 🔧 変更
- `build_analyzer_prompt()` の実装を `str.format()` から `str.replace()` 方式に変更（RFDと同パターン）。SCORES_JSON出力例に含む波括弧の事故と、ユーザー入力に `{}` が含まれる場合の事故を回避
- フォーム診断のmax_output_tokensを16384→24576に引き上げ（SCORES_JSON等の追加出力とthinkingトークンの共存のため。RFDと同基準）
- `requirements.txt` に `plotly>=5.24.0` を追加

## v1.10.2 (2026-07-03)

コードレビュー（`docs/code-review/sdt-2026-07-03.md`・ルートリポジトリ）に基づく修正。

### 🟢 改善（フェーズ3）
- スクリーナーの503エラーを整形メッセージ化（429と同様の分岐を追加）
- 不要な波括弧エスケープを削除：`.format()` の引数値は再走査されないため、ユーザー入力の `{` が `{{` に化けてAIに渡っていた劣化を解消
- 診断結果を保持中のSTEP 2で「1日1回まで」警告と再アップロード導線を非表示に。スキップボタンも隠し、取得済み診断結果の誤破棄を防止
- 破損・改ざんcookieの回数値を0扱いに（読み込み不能で `counts_loaded` が立たなくなる問題を解消）
- `use_container_width=True` を新API `width="stretch"` に移行（非推奨警告対応）。streamlit下限を1.49.0に引き上げ

### 🟡 修正（フェーズ2）
- **管理者ログインのfail-openを修正**：`ADMIN_PASSWORD` 未設定時に空パスワードでログインできていた。未設定時は常に拒否し、比較を `hmac.compare_digest` に変更（RFD v1.5.2と同パターン）
- **計画概要カードのHTML崩れ・自己XSSを修正**：自由入力のベストタイム・目標タイムを `html.escape` してから埋め込むよう変更
- **AIが `week` を文字列・欠落で返すと計画生成が全滅する問題を修正**：int変換ガード＋変換不能時は配列順で代替
- **和式日付「6月15日(水)」の曜日誤抽出を修正**：`_extract_weekday` が月の漢字「月」を曜日と誤認していた。括弧内の曜日を優先し、なければ末尾から走査
- **計画生成に全体タイムアウト（240秒）を追加**：APIハング時にプログレスバーが永遠に止まらない問題を解消
- **JSON途中切れ（MAX_TOKENS）時のリトライを廃止**：同条件で再試行しても同じ場所で切れるため、無駄なAPI課金（最大3回分）を防ぎ即エラー表示に変更
- リファクタ：STEP 2 / STEP 3 に重複していた計画生成処理（約60行×2）を `_run_plan_generation()` に統合

### 🔴 修正（フェーズ1）
- **計画週数が常に1週不足し、レース週が計画表から落ちるバグを修正**（`calculate_plan_weeks`）
  - **原因**: `today` の時刻成分を残したまま月曜同士の差を取っていたため、差が「k×7日−時刻」となり `timedelta.days` の切り捨てで週数が1週減っていた
  - **影響**: レースが最短週数より先の場合、火〜日曜レースはレース週が丸ごと欠落。月曜レースは「レース当日が落ちる」症状（v1.10.0時点の既知事象はこのバグの最軽症例だった）
  - **新**: `today` を0:00に正規化。月曜レースを含む全曜日でレース週が計画に含まれることをテストで確認
- **Gemini応答の本文が空（None）のときの障害を修正**（RFD v1.5.2のパターンを移植）
  - フォーム診断：1日1回の診断枠だけ消費して結果もエラーも出ない沈黙失敗 → finish_reason付きのエラーを表示し、枠は消費しない
  - 計画生成：`'NoneType' object has no attribute 'strip'` の生エラー表示 → 空応答を検知して日本語エラーに変換
  - スクリーナー：本文Noneで例外 → 既存のfail-open（診断に進む）へ統一
- **UTC/JST日付ズレを修正**：`jst_now()`（Asia/Tokyo）を導入し `datetime.today()`/`datetime.now()` を全置換
  - 月曜0:00〜9:00 JSTに作成すると計画が1週前倒しになる問題、使用回数の日次リセットが9:00 JSTになる問題、レース日入力・ダウンロードファイル名の日付ズレを解消

---

## v1.10.1 (2026-06-11)

### 🤖 Gemini 3.5 Flash 移行
- 計画生成モデル（planner）を `gemini-3-flash-preview` から正式版 `gemini-3.5-flash` に移行。
- 思考パラメータを `thinking_budget`（数値）から推奨の `thinking_level`（段階）へ移行。planner / analyzer とも深い推論を要するため `"high"` を指定。
- `temperature` / `top_p` を全リクエストから削除（planner・screener）。Gemini 3.x ではこれらサンプリングパラメータが非推奨化され、デフォルト設定が最適化済みのため（公式ドキュメント準拠）。
- `requirements.txt` の `google-genai` を `>=2.7.0` に引き上げ（`thinking_level` 対応バージョンを保証）。

---

## v1.10.0 (2026-06-04)

### 修正
- 計画の開始日がレース日から逆算され、作成日（今週）が計画から抜け落ちる問題を修正
  - **原因（主因）**: 計画表の日付は `_plan_json_to_markdown` の `start_dt` 計算ではなく、Gemini が独自採番していた。スキーマは `"date": "月"`（曜日名）を要求しているが、AI が `"2026-06-15(月)"` のような完全日付を返すと `_DAY_MAP` に該当せず、そのまま素通しされていた（コード側の日付管理がバイパスされ、AI が開始日を任意にずらせる状態だった）
  - **新**: `_extract_weekday()` を追加し、`date` 欄に何が入っていても曜日文字（月〜日）を抽出して `start_dt + 週/曜日オフセット` から日付を再計算。日付の正をコード側に固定し、AI が開始日をずらせないようにした
  - プロンプトのJSONスキーマ説明にも「`date` 欄は曜日名のみ・日付は書かない」を明記（二重防御）

### 変更
- `calculate_plan_weeks()` をAMC方式に変更（最短週数のみを下限に固定）
  - **旧**: `default_weeks`（例：800m=16週）を上限に切り捨て、レース週月曜日から `total_weeks` ぶん逆算 → 作成週が落ちて隙間が生じることがあった
  - **新**: レースまで十分な期間があれば「作成週の月曜日」から開始し、計画週数は作成週〜レース週の inclusive な実週数とする。レースが最短週数より近い場合のみ、最短週数を確保するため過去に遡って開始する。上限による切り捨ては廃止
  - これにより、レースが遠い場合でも作成日（今週）から計画が始まり、練習レース・記録会が計画内に正しく収まる
- 出力トークン上限を計画週数に比例させる `get_planner_max_tokens()` を追加（AMC方式）
  - 上限週数の撤廃により遠いレースで週数が増えても、固定 32768 トークンで JSON が途中切れしないよう、`週数×1500+4096`（モデル上限 65536 で頭打ち）で確保。16週以下は従来どおり 32768

---

## v1.9.0 (2026-06-03)

### 追加
- トレーニング計画（STEP3）の直下に、補強メニュー実践用の筋トレ・フィットネスグッズへ誘導するCTAカード（`render_gear_cta()`）を追加
  - 計画に含まれる筋力・プライオ・コア・ドリル種目に必要な用品をAmazonおすすめリストへ送客
  - CTRの高い「計画直後・ダウンロードボタンの前」に配置。フォーム診断（STEP2）側には置かない
  - リンク先URLは `config.py` の `AMAZON_FITNESS_LIST_URL` で管理（アフィリンクのため `rel="noopener noreferrer sponsored"` を付与）
  - 収益動線が無かった本アプリにアフィリエイト導線を新設

---

## v1.8.4 (2026-05-22)

### 修正
- STEP 2 ボタン押下後に画面がフリーズしたように見える問題を修正
  - **旧**: 「計画を作成する →」/「スキップして計画を作成する」押下 → `st.rerun()` で STEP 3 に遷移 → STEP 3 スクリプトが即座に 60 秒の while ループに入る → ブラウザは STEP 2 の dim overlay を表示したまま待機（プログレスバーが見えない）
  - **新**: ボタン押下時に中間 `st.rerun()` なしでインライン生成を実行し、同一スクリプト実行コンテキストでプログレスバーを表示。完了後に一度だけ `st.rerun()` して STEP 3 の結果表示へ遷移
  - `auto_generate_plan` フラグ・関連コードを削除
- 日曜日の練習レース・記録会が計画上で 1 週ずれる問題を修正
  - **旧**: プロンプトに「第 N 週・日曜日（M/D(日)）」と指定 → Gemini がアメリカ慣習（日曜 = 週初め）で解釈し、第 N+1 週の先頭（同日付）と判断して 1 週後に配置
  - **新**: `_parse_practice_races_with_dates()` で各週の月曜〜日曜の日付範囲（例：`6/8月〜6/14日`）を計算し、プロンプトに「第 N 週（M/D月〜M/D日）のX曜日（M/D(X)）」として週境界を明示。「週は月曜始まり・日曜終わりです（ISO 8601）」の注記も追加

---

## v1.8.3 (2026-05-22)

### 改善
- STEP 2 → STEP 3 遷移時にトレーニング計画生成を自動起動するよう変更
  - 「計画を作成する →」または「スキップして計画を作成する」を押すと、STEP 3 で「トレーニング計画を生成する」ボタンを押す手間がなくなった
  - `auto_generate_plan` フラグを導入し、STEP 2 ボタン押下時に True をセット → STEP 3 初回レンダリング時に自動起動
  - 概要カード（目標距離・タイム・計画期間等）は引き続き生成中に表示される

---

## v1.8.2 (2026-05-22)

### 修正
- 「練習レース・記録会」の日付が計画の正しい週・曜日に配置されない不具合を修正
  - **旧**: 練習レース日付の自由テキストをそのまま Gemini に渡し、「何週目か」の計算を Gemini に任せていた → LLM は週境界付近の日付計算が不得意で、土曜日のレースが前の週の日曜日に誤配置される
  - **新**: `plan_prompts.py` に `_parse_practice_races_with_dates()` を追加。Python 側で各レース日の週番号・曜日名を計算し、「第10週・土曜日（8/1(土)）」の形式でプロンプトに明示する。Gemini は日付計算不要でそのまま出力できる

---

## v1.8.1 (2026-05-22)

### 修正
- 「診断完了！」メッセージと「計画を作成する →」ボタンの表示タイミングのズレを修正
  - **旧**: `st.success("診断完了！")` を try ブロック内に配置 → `finally` の `cleanup_video()` 完了（約10秒）後にボタンが表示されるためタイムラグが発生
  - **新**: 両者を同一の `if form_diagnosis:` ブロック内で描画 → 動画削除完了と同時に「診断完了！」とボタンがまとめて表示される

---

## v1.8.0 (2026-05-22)

### 改善
- フォーム診断結果の表示タイミングをSTEP 2 → STEP 3に変更
  - STEP 2 では「診断完了！」のメッセージのみ表示し、長文テキストを出力しない
  - 「計画を作成する →」ボタンがスクロール不要で視認できるようになった
  - STEP 3（計画生成後）に「フォーム診断結果を確認する」エキスパンダーを追加
- チェックボックス「この診断結果をトレーニング計画に反映する」を廃止（診断実施＝反映を前提とする）
- STEP 2 のダウンロードボタンを廃止（STEP 3 の計画ダウンロードに診断結果が付記される既存機能で対応）

---

## v1.7.0 (2026-05-22)

### 修正
- 計画開始日・週数の計算ロジックを根本修正（`calculate_plan_weeks()`）
  - **旧**: `today` を起点に週数を計算 → 今日が月曜以外のとき端数が出て週数が過少になり、計画末尾がレース日を含まないズレが発生
  - **新**: `race_week_monday`（レース日を含む週の月曜）と `today_monday` の差を週数に使用し、開始日は `race_week_monday - (total_weeks-1)週` で逆算。計画の最終週の月曜日がレース日と常に一致する
  - 副次効果：練習レース日が正しい週に配置されるようになった
- 基本情報に「練習レース・記録会」を表示（入力がある場合のみ）

---

## v1.6.1 (2026-05-21)

### 改善
- サイドバーのデフォルト状態を「開いた状態」→「閉じた状態」に変更（PC表示）

---

## v1.6.0 (2026-05-21)

### 追加
- 練習レース・記録会の入力欄を追加（STEP 1・目標レース日の直後、任意）
  - 日付と大会名を改行区切りで入力可能（例：`6/1 春季記録会（400m）`）
  - 入力がある場合、Gemini プロンプトに反映され、指定日に試合を組み込んだ計画を生成

### 改善
- 週次スケジュール表の曜日列に実際の日付を表示（「月」→「5/19(月)」形式）
- 「要望・注意事項」ラベルの「（任意）」表示を青バッジスタイルに統一（練習レース欄と同形式）

---

## v1.5.0 (2026-05-21)

### 変更

- フォーム診断・計画生成の使用制限を「1日3回」→「1日1回」に変更（RFDと統一）
- 制限到達時の警告テキストを「このセッションの〜」→「1日あたりの〜明日またお試しください。」に更新
- Cookie書き込みオプションを追加（`same_site='none'`, `secure=True`, `partitioned=True`, `expires=+2日`）：iframe埋め込み時のクロスサイトCookie制限に対応

---

## v1.4.0 (2026-05-21)

### 変更

- フォーム診断モデルを `gemini-3.1-pro-preview` から `gemini-3.5-flash` に移行
- Gemini 3.5 Flash の仕様に合わせてアナライザーのAPIパラメータを更新
  - `temperature` / `top_p` / `top_k` を削除（3.5 Flash はデフォルト設定に最適化済みのため非推奨）
  - `thinking_budget` を 8192 → 16384 に増加（3.5 Flash の上限拡大を活用）
- スクリーニングモデル（gemini-3.1-flash-lite）・プランナーモデル（gemini-3-flash-preview）は変更なし

---

## v1.3.0 (2026-05-11)

### 改善
- 生成されるトレーニング計画の「基本情報」セクションに、STEP 1 で入力したすべての情報を反映
  - 追加フィールド：選手名（ニックネーム）・年齢・性別・目標レース日・週練習日数・トラック有無・ウェイトジム有無・要望/注意事項

---

## v1.2.0 (2026-05-11)

### 追加
- フォーム診断結果のダウンロード機能
  - STEP 2 の診断完了後に「診断結果をダウンロード（Markdown）」ボタンを追加
  - 既存の診断結果がある場合（STEP 2 に戻ったとき）も同ボタンを表示
  - STEP 3 の計画ダウンロードにフォーム診断結果を末尾付記（診断ありの場合のみ）
  - ファイル名：`sdt_form_diagnosis_YYYYMMDD.md`

---

## v1.1.0 (2026-05-11)

### 追加
- Cookie ベースの使用回数制限（フォーム診断・計画生成ともに1日3回）
  - ブラウザリロードでもカウントがリセットされない
  - 日付変更で自動リセット
  - 管理者ログイン時は制限解除

### ドキュメント整備
- `README.md` 新規作成
- `docs/user-manual.md` 新規作成
- `docs/spec.md`（技術仕様書）新規作成
- `CHANGELOG.md` 更新

---

## v1.0.0 (2026-05-11)

### 初回リリース
- 対象距離：100m / 200m / 400m / 800m / 1500m
- LLM ベース（Gemini）のトレーニング計画生成
  - Charlie Francis 式スプリント周期化理論（GPP → SPP → 競技期）に準拠
  - 距離ごとのエネルギーシステム情報をプロンプトに組み込み
  - JSON 出力 → Markdown 変換で週次スケジュール表を生成
  - Markdown ダウンロード機能（UTF-8 BOM でスマートフォン対応）
- ランニングフォーム診断（オプション）
  - 動画アップロード → スクリーニング → Gemini Pro + Thinking で解析
  - 診断結果（ドリル・筋力・モビリティ・プライオメトリクス・コア）を週次スケジュールに直接組み込み
  - 解析完了後に動画を自動削除
- 3ステップ UI（基本情報 → フォーム診断 → 計画生成）
  - ステップインジケーター表示
  - 計画概要カード（目標距離・タイム・期間・ベスト・練習日数・フォーム診断有無）
- セッション内使用回数制限（フォーム診断 3回・計画生成 3回）
- 管理者モード（サイドバーでパスワード認証、回数制限解除）
